<editors>

    <!-- Pusover User -->
    <editor id="PO_D_{0}">
      <range uom="25" subset="{1}" nls="POD_{0}" />
    </editor>

    <!-- Pusover Sound -->
    <editor id="PO_S_{0}">
      <range uom="25" subset="{2}" nls="POS_{0}" />
    </editor>

</editors>
